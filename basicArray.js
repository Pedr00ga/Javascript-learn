
const num = [1, 2, 3, 4, 5]

num.push(6, 7, 8) // adiciona no final

num.unshift(0) // adiciona no inicio

num.splice(3, 0, 2.5) //adiciona no meio colocando index, se quero deletar ou não e o numero

console.log(num)